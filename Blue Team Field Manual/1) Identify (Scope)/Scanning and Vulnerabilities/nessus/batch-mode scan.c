#include <stdio.h>
#include <stdlib.h>
int main(){
    system("clear");
    printf("I don't care enough to make an executable program for this, but here's the syntax: \n\n");
    printf("nessus -q [-pPS <HOST> <PORT> <USERNAME> <PASSWORD> <targets-file> <result-file>\n");
    return(0);
}
